commit_regex='^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)(\(.+\))?!?: .{1,}'
if ! grep -qE "$commit_regex" "$1"; then
  echo "Aborting commit: message must follow Conventional Commits." >&2
  echo "Example: feat: add reviewer queue" >&2
  exit 1
fi
